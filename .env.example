PORT=8080
DATABASE_PATH=/data/nudge.db
SECRET_KEY=change-me

FROM_NAME=
FROM_EMAIL=

# Optional SMTP. Leave SMTP_HOST unset to copy/export sequences without sending.
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=
# SMTP_TLS=true

# Optional footer link labeled "Powered by Nudge". Leave empty for no footer.
MARKETING_URL=
